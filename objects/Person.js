let Person = {
    name: 'Tony Stark',
    age: 50,
    location: 'New York and Los Angeles'
}

console.log(`${Person.name} is ${Person.age} and lives in ${Person.location}`)

Person.age += 1

console.log(`${Person.name} is ${Person.age} and lives in ${Person.location}`)
